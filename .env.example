# VinSight Environment Variables Template
# Copy this file to .env and fill in your values

# --- Environment (REQUIRED for Production) ---
# Set to "production" for production deployments
ENV=development

# --- Security (REQUIRED) ---
# Generate a secure random string (e.g., `openssl rand -hex 32`)
# REQUIRED in production - app will fail to start without it
JWT_SECRET_KEY=your_secure_random_key_here

# --- AI Services (At least one required for Full Features) ---
# Get from https://console.groq.com/
GROQ_API_KEY=

# Get from https://aistudio.google.com/
GEMINI_API_KEY=

# --- Stock Data (REQUIRED) ---
# Get from https://api-ninjas.com/
API_NINJAS_KEY=

# --- Alpha Vantage News (Optional - Recommended for better sentiment) ---
# Get FREE key from https://www.alphavantage.co/support/#api-key
# Provides news with built-in sentiment scores and article summaries
ALPHA_VANTAGE_API_KEY=

# --- Finnhub (Optional - For better insider sentiment) ---
# Get FREE key from https://finnhub.io/register
# Provides MSPR (Monthly Share Purchase Ratio) for insider activity
FINNHUB_API_KEY=

# --- Database (Optional - Defaults to local SQLite) ---
# Format: postgresql://user:password@host:port/dbname
# DATABASE_URL=

# --- Email (Optional - For Alerts/Verification) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
EMAIL_FROM=your_email@gmail.com

# --- Production CORS (Required for Production) ---
# Comma-separated list of allowed origins
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
